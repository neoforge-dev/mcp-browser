# MCP Browser Environment Configuration

# MCP Secret - Required for authentication
# This should be a secure random string in production
MCP_SECRET=your_secret_key_here

# Server Port - Optional, defaults to 7665
SERVER_PORT=7665

# Playwright Configuration - Optional
# Set to 1 to skip browser download and run in headless-only mode
# PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1

# Log Level - Optional
# Set the logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO 